"""Provider abstraction.

TODO(britney): Hand-write this file.

What lives here:
  - A normalized response model that every provider returns.
  - An abstract base class that defines the contract every provider must implement.

Do NOT peek at the reference version at git tag `v0-bootstrap-reference`
until you've finished a first pass and explained your choices out loud
(the teach-back protocol).
"""
